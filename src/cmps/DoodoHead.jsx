import React from 'react';
import { connect } from 'tls';

class DoodoHead extends React.Component {
    state = {
    }
    async componentDidMount() {

    }

    render() {
        return (
            <>
            <h1>DoodoHead</h1>
            </>
        )
    }
}

const mapStateToProps = (state) => {
    return {
    }
}

const mapDispatchToProps = {
}

export default connect(

)(DoodoHead)